using MiniAtraShared.Extensions;
using MiniAtraShared.Models;

namespace MapAndTileProperties;

/// <inheritdoc/>
internal class ModEntry : BaseMod<ModEntry>
{
    /// <inheritdoc/>
    public override void Entry(IModHelper helper) => base.Entry(helper);
}
